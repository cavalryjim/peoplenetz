<% content_for :title, "Contact Us" %>
<%= raw display_banner %>

<%= render "layouts/begin_content"%>

<div class="row-fluid">
  <div class="span9">
	<h4><%= t "contact.header", default: "Request More Information" %></h4>
	<p><%= t "contact.under_header", default: "We would like to hear from you." %></p>
  	<% flash.each do |key, value| %>
      <div class="alert alert-success">
		<button class="close" data-dismiss="alert">×</button>
		<strong><%= value %></strong>
	  </div>
    <% end %>
	<%= render 'form' %>
	
  </div>

  <div class="span3">
	<%= render 'contact_info' %>
  </div> <!-- span -->
</div>

<%= render "layouts/end_content" %>
